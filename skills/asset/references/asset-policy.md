# Asset Review Policy

- Treat image files over 300KB as release-blocking unless the change explicitly documents a justified exception.
- Prefer compressed raster formats for photos and vector formats for simple icons.
- For binary diffs, use file metadata such as `file_size_bytes`, `file_size_kb`, and `change_type` as primary evidence.
